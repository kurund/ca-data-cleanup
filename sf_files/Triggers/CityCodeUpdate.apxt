/* set city code after creating new city*/
trigger CityCodeUpdate on City_Master__c (After insert) {
   for(City_Master__c city:trigger.new){
      TriggerCheck triggerCheck = New TriggerCheck();
      triggerCheck.calculateCityCode(city.id);
   }
}